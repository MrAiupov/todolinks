Создай одностраничное HTML-приложение Todolinks — локальный менеджер ссылок и задач с единым списком. Хранилище данных — IndexedDB.

=== 1. ОБЩИЕ ТРЕБОВАНИЯ ===
- Весь код в одном файле index.html (HTML, CSS, JS).
- Открывается двойным кликом, без сервера, Node.js, Docker.
- Хранилище: IndexedDB (база данных TodolinksDB, версия 2).
- Чистые HTML/CSS/JS, без фреймворков и CDN.
- Две темы: светлая / тёмная (переключатель — одна круглая кнопка ☀/☾).
- Адаптивный дизайн (ПК, планшет, мобильные). На мобильных устройствах строки имеют фиксированную высоту, лишний текст скрывается (text-overflow: ellipsis), приоритет контента: название > теги > описание.
- Название вкладки: «Todolinks · ссылки и задачи».
- Верхняя панель и статистика закреплены, прокручивается только список.
- При фокусе на поиске — мягкое изменение фона на `var(--accent-muted)` и границы на `var(--accent)`, без обводки (outline: none).
- При загрузке страницы — автоматический фокус на поле поиска (через setTimeout 50ms).
- **Защита от белого мерцания:** скрипт в `<head>` проверяет localStorage и применяет класс `dark` к `<html>` до отрисовки CSS. CSS-переменные определены на `:root` (и `html.dark`), а не на `body`.
- **При сужении окна поисковая строка сжимается, но кнопки остаются на одной линии** (не переносятся). Используется `flex-shrink: 1` для поиска и `flex-shrink: 0` для кнопок.

=== 2. СТРУКТУРА ДАННЫХ (IndexedDB) ===
База данных: TodolinksDB, версия 2.
Хранилище объектов: items (ключ: id).
Индексы: type, todoStatus, dueDate, pinned, createdAt.

Запись (ссылка):
{
  id: "uuid", type: "link", title: "GitHub", url: "https://github.com",
  tags: ["работа"], pinned: false, description: "", color: null,
  createdAt: "ISO8601"
}

Запись (задача):
{
  id: "uuid", type: "todo", title: "Купить молоко", description: "",
  tags: ["дом"], pinned: false, todoStatus: "active", dueDate: "2026-05-01",
  completedAt: null, repeat: null, color: null,
  createdAt: "ISO8601"
}

Поля:
- repeat: null | "daily" | "weekly" | "monthly" | "yearly" — повтор задачи
- color: null | "1"..."9" — цвет фона строки

=== 3. ВЕРХНЯЯ ПАНЕЛЬ ===
Слева направо:
1. Поисковая строка (растягивается, flex-shrink: 1, min-width: 120px). Placeholder: «Поиск, ссылка или задача...»
2. Кнопка + (круглая, синяя, flex-shrink: 0) — открывает форму новой ссылки.
3. Кнопка Экспорт ↑ (круглая, зелёная, flex-shrink: 0).
4. Кнопка Импорт ↓ (круглая, красная, flex-shrink: 0).
5. Кнопка темы ☀/☾ (круглая, серая, flex-shrink: 0).

**При сужении окна кнопки не переносятся на новую строку, сжимается только поисковая строка.**
Для этого используется `flex-wrap: nowrap` на .top-bar и `min-width: 0` на поиске.

Все круглые кнопки: 36x36px, border-radius 50%, display:flex, align-items:center, justify-content:center, font-size:1.2rem, font-weight:bold, border:none, color:white, transition:opacity 0.1s.
Цвета: + (--accent), Экспорт (--export-bg), Импорт (--import-bg), тема (--button-bg).

=== 4. ФИКСИРОВАННАЯ ШАПКА ===
.app: max-height: 100vh, overflow: hidden, flex-колонка.
.top-bar, .stats-bar: flex-shrink: 0.
.list-container: overflow-y: auto, flex: 1.

=== 5. СТАТИСТИКА ===
Три равные колонки (flex с равными flex: 1):
- Левая: текстовые кнопки «GitHub» и «Поддержать» (без иконок, цветной текст, полупрозрачность).
- Центр: «Всего: X ссылок, Y задач (Z выполнено)».
- Правая: текстовые кнопки «Помощь» и «Удалить» (без иконок, цветной текст, полупрозрачность).

Стиль всех текстовых кнопок: background: transparent, border: 1px solid transparent, opacity: 0.8, при наведении opacity: 1 и фон --accent-muted.
Цвета: GitHub (--text-secondary), Поддержка (--delete-color), Помощь (--accent), Удалить (--delete-color).

=== 6. УМНАЯ СТРОКА ===
- **Новая логика Enter:**
  - **Enter** — поиск введённого текста в Google (https://www.google.com/search?q=...).
  - **Ctrl+Enter** — открыть форму добавления (ссылка или задача, определяется автоматически).
  - **Кнопка +** — открыть форму добавления (ссылка по умолчанию).
- **Конвертация раскладки клавиатуры:** при поиске автоматически генерируется альтернативный вариант запроса в другой раскладке (русская ↔ английская). Это позволяет находить элементы и команды, даже если пользователь забыл переключить язык.
- Live-поиск по title, url, description, tags с учётом обоих вариантов раскладки.
- Поиск выполненных задач: «done», «готово», «выполнено», «completed», «finished» и т.п. (если одно из слов есть в запросе, показываются только завершённые задачи).
- Автоопределение типа:
  - URL (начинается с http://, https:// или содержит домен вида example.com) → ссылка.
  - Содержит дату YYYY-MM-DD или @тег → задача.
  - Обычный текст → задача.

=== 7. БЫСТРЫЙ ПОИСК НА САЙТАХ ===
Старые ключевики **сохранены как дополнительный способ**. Теперь основной способ поиска на других сайтах — набрать ключевик и запрос, нажать Enter (но проще просто ввести запрос и нажать Enter для Google).

Ключевики и URL:
- яндекс, yandex → https://yandex.ru/search/?text=
- гугл, google → https://www.google.com/search?q=
- ютуб, youtube → https://www.youtube.com/results?search_query=
- рутуб, rutube → https://rutube.ru/search/?q=
- озон, ozon → https://www.ozon.ru/search/?text=
- валберис, wildberries, вб, wb → https://www.wildberries.ru/catalog/0/search.aspx?search=
- маркет, market → https://market.yandex.ru/search?text=
- вики, wiki → https://ru.wikipedia.org/w/index.php?search=
- гитхаб, github → https://github.com/search?q=
- переводчик, перевод, translate → https://translate.yandex.ru/?text=
- нейросеть, нейро, perplexity, перплексити → https://www.perplexity.ai/search?q=

=== 8. ПОВЕДЕНИЕ ПО ENTER (приоритет) ===
1. **Enter** → поиск в Google (основное поведение).
2. **Ctrl+Enter** → открыть форму добавления (ссылка или задача).
3. **Кнопка +** → открыть форму добавления.
4. Проверка на поисковик/переводчик (с конвертацией раскладки) → открыть поиск/перевод в новой вкладке (дополнительный способ).
5. Активный элемент (keyboard-active) → открыть ссылку в текущей вкладке / переключить задачу (с учётом повтора).
6. Пустой ввод → открыть форму новой ссылки.
7. Есть результаты поиска внутри Todolinks → Enter всё равно выполняет поиск в Google.

=== 9. НАВИГАЦИЯ С КЛАВИАТУРЫ ===
- ↓/↑ — подсветка строк (класс .keyboard-active, фон --active-row). **Бесконечная прокрутка:** ↓ на последнем элементе → первый, ↑ на первом → последний.
- Enter на ссылке — открыть URL в текущей вкладке.
- Enter на задаче — переключить чекбокс выполнения (с учётом повтора).
- Shift+Enter — редактирование.
- Esc — снять выделение, очистить поиск.

=== 10. ЕДИНЫЙ СПИСОК ===
Сортировка (плоская, без вложенности):
1. Закреплённые (pinned) в начале своей группы.
2. Активные задачи со сроком (по dueDate, ранние сверху).
3. Активные задачи без срока.
4. Ссылки.
5. Выполненные задачи (в конце).

Строка (всё в одну линию, gap 0.6rem, **фиксированная высота 40px**):
- Чекбокс выполнения (только задачи, accent-color: --accent, filter grayscale(0.3) opacity 0.8).
- Заголовок: для ссылок — кликабельный текст с атрибутом title, показывающим URL; для задач — обычный текст (выполненные — зачёркнуты). **URL в строке не отображается.**
- Срок (для задач). Если просрочен — класс .overdue.
- Описание (max-width: 300px, text-overflow: ellipsis). **На мобильных скрывается первым.**
- Теги (справа). Отображаются **всеми заглавными буквами** (CSS: text-transform: uppercase). **На мобильных скрываются вторыми.**
- Чекбокс массового выбора (правый).
- Кнопка закрепа: ⚐ (белый флаг) / ⚑ (красный флаг). Кнопка фиксированной ширины 28px.
- Кнопка ✎ (редактировать, цвет --text-secondary).
- Кнопка ✕ (удалить, цвет --delete-color).

**Мобильная адаптация строк:** фиксированная высота, flex-wrap: nowrap, приоритет: заголовок > теги > описание. При сужении скрываются через display: none в медиа-запросах.

Двойной клик по строке открывает редактирование элемента.

Повторяющиеся задачи:
- При отметке чекбокса (выполнении) задача не уходит в архив, а её dueDate обновляется по правилу (используется UTC для надёжности):
  - daily: +1 день (setUTCDate)
  - weekly: +7 дней
  - monthly: +1 месяц с сохранением числа месяца. Вычисляется через Date.UTC для предотвращения сдвигов часовых поясов.
  - yearly: +1 год
- Статус задачи остаётся active, completedAt не устанавливается.
- При снятии чекбокса задача переходит в активный статус.

Цвета строк:
- Выбор в форме редактирования из списка:
  По умолчанию, Мятный, Розоватый, Голубоватый, Небесный, Персиковый, Сиреневый, Дымчатый, Лимонный, Румяный
- Применяются как дополнительный фон строки (класс .color-1 … .color-9).

=== 11. МАССОВЫЕ ОПЕРАЦИИ ===
При выборе чекбоксов (через Shift можно выделить диапазон) появляется нижняя панель .bulk-bar с удалением и снятием выделения.

=== 12. ФОРМА ДОБАВЛЕНИЯ / РЕДАКТИРОВАНИЯ ===
Открывается по кнопке + или Ctrl+Enter.
Переключатель Ссылка / Задача.
Поля:
- Название *
- URL * (ссылка). При вводе автоматически добавляется https://, если протокол не указан.
- Срок (задача)
- Повтор (задача): Без повтора / Ежедневно / Еженедельно / Ежемесячно / Ежегодно
- Цвет (селект с названиями).
- Теги (через запятую). При вводе показываются подсказки (.tag-suggestions) с уже существующими тегами из базы. При клике на подсказку тег добавляется. Вводимые и отображаемые теги приводятся к верхнему регистру.
- Описание
Кнопки: Отмена / Сохранить.

=== 13. ИМПОРТ / ЭКСПОРТ ===
- Экспорт (↑):
  - Используется File System Access API (showSaveFilePicker) с предложенным именем Todolinks_YYYYMMDD_HHMMSS.json.
  - **Важно:** Если API не поддерживается (например, в Safari), реализован fallback — стандартное скачивание через `<a download>`.
- Импорт (↓): модалка «Заменить все» / «Добавить к существующим».

=== 14. МОДАЛЬНЫЕ ОКНА ===
- Единый стиль: белый/тёмный фон, скруглённые углы, мягкая тень.
- Подтверждение удаления всего и удаления одной записи.
- Окно «Помощь» с кратким руководством: умная строка, быстрые поисковые команды (таблица с ключевиками), горячие клавиши, хранение данных, упоминание о работе поиска при неверной раскладке.

=== 15. ДОПОЛНИТЕЛЬНО ===
- Toast-уведомления (3.5с).
- Пустое состояние: «Нет ссылок или задач. Добавьте первую.»
- Автофокус при загрузке: setTimeout(() => smartInput.focus(), 50).
- Внешние ссылки (GitHub, Поддержать) открываются в новой вкладке.
- **Защита от мерцания:** скрипт в `<head>` немедленно применяет тему до загрузки CSS.
- **Кнопки верхней панели не переносятся на новую строку при сужении окна.** Сжимается только поисковая строка.

=== 16. ДИЗАЙН (СТРОГИЙ МИНИМАЛИЗМ, КОМПАКТНЫЙ) ===
Светлая тема (переменные на :root):
  --bg: #f5f5f5, --surface: #ffffff, --text: #1e1e1e, --text-secondary: #555555
  --border: #d0d0d0, --accent: #3a6ea5, --accent-muted: #e2eaf2
  --pinned-bg: #f0f3f7, --tag-bg: #e8e8e8, --delete-color: #c0392b
  --export-bg: #2e7d32, --import-bg: #c0392b
  --pin-active: #c0392b, --active-row: #d0e0f0

Цвета строк (светлая):
  1 (Мятный): #e8f5e9
  2 (Розоватый): #fce4ec
  3 (Голубоватый): #e3f2fd
  4 (Небесный): #e0f7fa
  5 (Персиковый): #fff3e0
  6 (Сиреневый): #f3e5f5
  7 (Дымчатый): #e8eaf6
  8 (Лимонный): #f9fbe7
  9 (Румяный): #ffebee

Тёмная тема (html.dark):
  --bg: #1a1a1a, --surface: #262626, --text: #e0e0e0, --border: #444444
  --accent: #6a9ac5, --accent-muted: #2a3a4a, --pinned-bg: #2d333b
  --tag-bg: #3a3a3a, --delete-color: #e74c3c
  --export-bg: #4caf50, --import-bg: #e74c3c
  --pin-active: #e74c3c, --active-row: #3a5068

Цвета строк (тёмная):
  1: #1e3a2a
  2: #4a2a33
  3: #1a2a4a
  4: #1a3a4a
  5: #4a3520
  6: #3a2a4a
  7: #2a2a4a
  8: #3a4a1a
  9: #4a2020

Круглые кнопки: 36x36px, белый текст на цветном фоне.
Чекбоксы: filter grayscale(0.3), opacity 0.8.
Иконки: ✎ (--text-secondary), ✕ (--delete-color).
Текстовые кнопки (GitHub, Поддержка, Помощь, Удалить): без иконок, только текст, цветной, полупрозрачный.
Кнопка закрепа: фиксированная ширина 28px, цвет --text-secondary (⚐) / --pin-active (⚑).

=== 17. ПРИОРИТЕТ КОНТЕНТА НА МОБИЛЬНЫХ ===
@media (max-width: 650px):
  .item-row { height: 40px; flex-wrap: nowrap; overflow: hidden; }
  .item-description { display: none; }  /* скрывается первым */
  .item-tags { display: none; }         /* скрывается вторым */
  (название и кнопки всегда видны)

=== 18. РЕЗУЛЬТАТ ===
Полностью готовый автономный HTML-файл index.html.
